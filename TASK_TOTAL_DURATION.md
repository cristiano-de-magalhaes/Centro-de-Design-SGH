```
={"TASK TOTAL DURATION";
        ARRAYFORMULA(
                IF(ISBLANK(B2:B);"";                
                        IF(ISBLANK(G2:G);TIME(0;0;0);
                        TIME(
                                 0;
                                 LEFT(G2:G;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(H2:H);TIME(0;0;0);
                        TIME(
                                 1;
                                 LEFT(H2:H;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(I2:I);TIME(0;0;0);
                        TIME(
                                 2;
                                 LEFT(I2:I;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(J2:J);TIME(0;0;0);
                        TIME(
                                 3;
                                 LEFT(J2:J;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(K2:K);TIME(0;0;0);
                        TIME(
                                 4;
                                 LEFT(K2:K;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(L2:L);TIME(0;0;0);
                        TIME(
                                 5;
                                 LEFT(L2:L;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(M2:M);TIME(0;0;0);
                        TIME(
                                 6;
                                 LEFT(M2:M;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(N2:N);TIME(0;0;0);
                        TIME(
                                 7;
                                 LEFT(N2:N;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(O2:O);TIME(0;0;0);
                        TIME(
                                 8;
                                 LEFT(O2:O;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(P2:P);TIME(0;0;0);
                        TIME(
                                 9;
                                 LEFT(P2:P;2);
                                 0)
                        )
                        +
                        IF(ISBLANK(Q2:Q);TIME(0;0;0);
                                TIME(
                                         10;
                                         LEFT(Q2:Q;2);
                                         0)
                        )
                )
        )
}
```
